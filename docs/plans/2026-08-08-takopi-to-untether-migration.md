# Takopi to Untether Migration Execution Plan

## Context
Task 24 ports every implemented Takopi enhancement from immutable read-only source `D:/Projects/takopi` at `481f0c72f50ef5b7a7793f547059d691f7b8ffc3` into the final writable Untether destination `D:/Projects/untether` at baseline `4285dad5a12e4e4113c9cc5240972a67bbb5e218`. `D:/Projects/untether-review` is a read-only comparison/reference checkout at the same Untether SHA and is never an implementation target. The finished `littlebearapps/untether` tree must retain Untether’s existing feature surface while preserving Takopi’s hardened scheduler, Telegram queue behavior, engine/schema additions, subprocess invariants, cross-platform CI, hard typing, and coverage gate.

## Approach
Apply Ponytail’s full ladder throughout: adapt existing seams in the writable destination `D:/Projects/untether`, port only source-verified Takopi behavior, and add no compatibility package, parallel scheduler, wrapper registry, speculative option, or new dependency. Treat both `D:/Projects/takopi` and `D:/Projects/untether-review` as read-only for the entire execution; read Takopi for source behavior and `untether-review` only to reproduce the approved baseline/comparison evidence. Before editing, verify `D:/Projects/untether` still matches destination SHA `4285dad5a12e4e4113c9cc5240972a67bbb5e218` with a clean tree and Takopi still matches the source SHA; stop and regenerate affected evidence if either differs. Write/adapt the named behavioral tests before each production slice, make the focused tests pass, then commit that slice in `D:/Projects/untether` only.

1. **Freeze the destination preservation baseline.** Run the pre-change commands in **Verification** and retain their exact output. Keep the `untether` namespace, Python `>=3.12`, MIT license, existing dependencies, package/config/plugin architecture, pinned actions, security/release jobs, and all destination engines/subsystems. Do not copy Takopi’s `pyproject.toml`, configuration model, registries, or base runner wholesale. Add no generic “migration framework”; the existing focused Untether tests are the preservation contract.

2. **Port scheduler-owned pending/claimed state.** In `src/untether/scheduler.py`, preserve existing `ThreadJob` fields and add Takopi’s consumed fields: `plan: bool = False`, `goal: str | None = None`, `kind: Literal["prompt", "compact", "handoff"] = "prompt"`, `compact_instructions: str | None = None`, and `handoff_target: EngineId | None = None`. Port `JobClaimed`, `JobFailed`, `EnqueueDisposition` (`queued`, `claimable`), `CancelQueuedStatus` (`cancelled`, `already_claimed`, `not_found`), and `CancelQueuedResult`; only `CANCELLED` may carry the removed job, enforced in `__post_init__`. Extend `ThreadScheduler.__init__` with optional async claim/failure observers. Make `enqueue()` and `enqueue_resume()` return `EnqueueDisposition`; port `claim_queued()`, `requeue_front()`, locked queue queries, and the `_claimed_by_progress` index. The atomic worker transition removes `(chat_id, progress_message_id)` from pending and installs it in claimed under the same lock; cancellation removes pending jobs only. `ResumeToken(engine, value)` remains the scheduling identity—Untether’s `is_continue` is invocation metadata, not a new session. Preserve worker-start rollback, observer-failure isolation, and continuation after a failed job. Adapt `tests/test_scheduler_queue.py` with deterministic AnyIO event barriers for FIFO, exact identity, cancel-before/after-claim, stale callbacks, claim/requeue, enqueue disposition, rollback, observers, failure continuation, and result invariants.

3. **Make Telegram cards reflect scheduler truth.** Update every old `ThreadJob | None` cancellation branch in `src/untether/telegram/commands/cancel.py` to handle all three statuses. `CANCELLED` terminates only the returned job’s progress card; `ALREADY_CLAIMED` reports that it started; `NOT_FOUND` reports a stale/already-handled card. Neither non-cancelled path touches the active run. Port queued steering with `claim_queued()` and `requeue_front()` on transfer failure. Add `RunnerTurnControl | None` to `runner_bridge.py:RunningTask`, preserve control extraction and interrupt-on-cancel, and port the source runners’ implemented control emitters; without an actual control instance `_thread_turn_steerable()` returns false and the card has Cancel only. In `telegram/loop.py`, adapt `_make_scheduler_observers` to Untether’s existing client/outbox, pass observers at scheduler construction, clear obsolete controls when claimed, and render visible failures on the same card. Build queued UI from `EnqueueDisposition`: every pending card has Cancel; Steer appears only when the active turn accepts it. Add `/queue` as an Untether command plugin backed by scheduler queries and register it in handlers, built-in dispatch, and the menu. Preserve cancel deduplication, sender authorization, `/at` and `/loop` fallbacks, queued wait notes, permission-button merging, progress refresh, and outbox `SUPERSEDED`. Put cancel/steer tests in new `tests/test_telegram_cancel_steer.py`; keep existing outbox `tests/test_telegram_queue.py` unchanged and green.

4. **Form one run-options superset.** Extend `src/untether/runners/run_options.py:EngineRunOptions`; retain all destination fields and add Takopi’s `PromptAttachment`, `attachments`, `plan`, `goal`, `skill`, and `subagent`. Port `merge_run_options()` and extend it to preserve every destination field; supplied values override, omitted values inherit, goal is stripped/empty-to-`None`, and attachments become tuples. Update `runner_bridge.py`, Telegram option resolution/trigger permission override, commands, upload dispatch, queue reconstruction, and trigger dispatch to merge rather than rebuild options. Preserve destination precedence: trigger permission override, then topic/chat engine overrides, then per-run augmentation, then runner configuration fallback. Port only implemented directive/state behavior for plan, goal, skill, and subagent into Untether’s existing command backend—no second registry. Preserve duplicate/missing-value errors, sticky subagent clear/off semantics, explicit-over-sticky precedence, free-form skill carriage, and goal-over-plan precedence. Port `src/takopi/runners/modes.py` as `src/untether/runners/modes.py`, changing the user-facing prefix to Untether and reusing its prompt helpers in applicable runners. Adapt run-option, image, plan/goal, directive, and subagent tests; add assertions that merging never drops Untether’s permission, diff, cost, budget, resume, usage, or loop fields.

5. **Port file-backed prompts and prompt batching.** Port `src/takopi/telegram/prompt_batch.py` and connect it to Untether’s existing message coalescing/dispatch path. Add the source settings through `TelegramTransportSettings` → backend → `TelegramBridgeConfig`, with source-verified `prompt_batch_enabled=True` as the default and explicit `False` as the opt-out. Keep message-id ordering, the inspected batch key (chat, topic/thread, sender, reply target), limits, and newline/blank-line separators. Control commands, media/voice, and forwarded-only messages never batch; interpret directives once after assembly and dispatch one run. Preserve Task 22’s non-image file wording exactly as `Execute the task specified in this file: \`<path>\`.` while retaining Untether file security, size, and delivery behavior. Adapt both prompt-batch test modules and image/file tests for enabled default, explicit disabled opt-out, ordering, isolation, limits, control-command boundaries, attachments, and exactly one dispatch.

6. **Port honest compact/handoff lifecycle.** Create `src/untether/compact.py` from Takopi’s implemented module, retaining `CompactSupport`, `CompactRunner`, `CompactUnsupportedError`, the five inspected modes, instruction normalization, prompt helpers, and capability validation. Integrate commands through Untether’s command plugins. Native-capable `/compact` compacts the existing session; unsupported/handoff-only engines resolve the destination before showing approve/cancel, then summarize the old session, seed the resolved new session with the full summary, and atomically route future turns to the new token. `/handoff` always uses that approved old→new flow. Queue these as `ThreadJob.kind` on the same resume key; preserve directive ordering and reply-session lookup. Every accepted operation emits acknowledgement plus completion or visible failure; `CompletedEvent(ok=False)` is failure. Port `_compact_mixin.py` and `_acp.py`, adapting them to Untether’s `manage_subprocess`. Keep ACP JSON-RPC 2.0, protocol 1, one request in flight, 10 MiB line limit, timeouts, ID matching, `-32601` unsupported-request replies, and advertised `compact` capability gating. OMP/Grok remain `handoff_only` absent live proof of native compaction; their deterministic handoff behavior is required and is not contingent on live credentials. Adapt all compact/handoff/ACP tests, including `tests/test_compact_event_invariants.py`, and assert one Started, zero-or-more Action, one final Completed, with identical Started/Completed resume tokens.

7. **Make Pi forward-compatible, then add OMP.** In `src/untether/schemas/pi.py`, preserve Untether auto-compaction events, allow numeric `AutoRetryStart.delayMs`, and add `PiUnknownEvent`, `_TAGGED_EVENT`, `_KNOWN_TYPES`, `_PEEK_DECODER`, and `decode_event()`: unknown string tags return `PiUnknownEvent`; malformed JSON/type and malformed known events still raise. Merge Takopi’s `PiStreamState` controls (`shorten_session_id`, `allow_id_promotion`, `last_usage`, `last_assistant_text`) into Untether’s richer Pi/JSONL state without removing watchdog/recent-event behavior. In the Pi runner, log each unknown type once per run at DEBUG and retain Pi’s shortened display. Port `src/takopi/resume_parse.py` for shared resume alias parsing only where existing destination parsing calls it. Port `runners/omp.py` and entry point `omp = "untether.runners.omp:BACKEND"`; instantiate `PiStreamState(shorten_session_id=False, allow_id_promotion=True)` for new/resumed runs and retag every token/event to `omp`, preserving `is_continue=False` unless the incoming destination token explicitly carries it. Preserve invocation arguments, provider/model/reasoning, attachments, plan modes, and terminal errors. Add OMP only to proven capability maps. Adapt Pi schema/runner and OMP tests for float delay, unknown/strict known decode, full UUID tokens/footer, resume, and `rc=0` capacity failure.

8. **Add Grok and Agy at existing runner seams.** Port `schemas/grok.py`, `runners/grok.py`, `runners/agy.py`, and their entry points without replacing Untether’s richer runner base. Preserve Grok streaming/narration coalescing, final narration, tool titles, action closure, native read-only plan flags, non-empty cancellation-plan salvage, run options, honest errors, and handoff-only status. Preserve Agy CLI/session/schema behavior, goal prefix, event ordering, and handoff-only status. Port verified native flags only. Port Takopi’s transient-failure classifier into Untether’s shared failure normalization: retry only classified transient failures before any side-effectful action, bounded by existing/source settings; cancellation and non-transient failures pass through. Preserve 429/503, `retry-after-ms`, admission/capacity, overload, rate-limit, and `rc=0` terminal classifications. Emit one clean terminal failure, never raw JSON or duplicate cards. Adapt Grok/Agy/schema/transient tests and stored fixtures.

Before editing new runners, inspect `src/untether/runner.py` and keep its stall detector, approval refire, path/URL redaction, recent-event ring, and watchdog loop. Add only the compact hooks/settings/build-argument seams the new runners consume. Verify `src/untether/tool_actions.py`, `events.py`, `ids.py`, and destination runner-settings parity; reuse matching symbols and explicitly adapt mismatches. Do not copy Takopi’s base runner. Add new engine config keys through Untether’s existing runner/settings schema and validation; do not create parallel per-engine settings models.

9. **Merge Windows cleanup into Untether process management.** Reconcile `src/untether/utils/subprocess.py`; do not replace its environment redaction/isolation, diagnostics, descendant signaling, resource guards, active-process tracking, POSIX groups, or orphan reaping. Add Takopi’s bounded stream closure and Windows tree kill (`taskkill /PID <pid> /T /F`, direct-kill fallback), and ensure managed processes are reaped before stdin/stdout/stderr close. Route runner, Codex control, ACP, and trigger process ownership through the reconciled helper instead of duplicating cleanup. Adapt subprocess-close tests; on Windows 3.14, interrupted child execution must produce neither `Exception ignored` nor `unclosed transport`; on POSIX preserve group/orphan behavior.

10. **Harden quality gates without deleting destination gates.** Keep Python `>=3.12`; rewrite incompatible Takopi syntax instead of raising the floor. Resolve destination source/test diagnostics and make `uv run --no-sync ty check src tests` mandatory by removing the informational/`allow_failure` treatment. Preserve pinned actions, least permissions, docs/lockfile/build/install/TestPyPI/security/release-validation jobs. Run format, Ruff, hard ty, and no-coverage tests on Ubuntu/macOS/Windows with Python 3.14 and `PYTHONUTF8=1`; retain Ubuntu 3.12/3.13 compatibility cells; run coverage once on Ubuntu 3.14. Guard POSIX-only imports at implementation boundaries so Windows collects the suite. Raise coverage from 80% to 81%, adding only behavioral tests for migrated branches. Extend install smoke for OMP/Grok/Agy and compact APIs. Preserve `src/untether/api.py` exports and add only the compact/scheduler types plugins need; keep the destination’s existing `TAKOPI_PLUGIN_API_VERSION = 1` literal rather than creating an alias package or second API version.

11. **Run preservation and end-to-end gates.** Execute **Verification** in order. Any regression in existing Untether triggers, costs, quarantine, hot reload, outbox/delivery, commands, environment policy/audit, diagnostics, systemd, migrations, persistence, stats, worktrees, plugins, Gemini, Amp, Claude, Codex, OpenCode, or Pi fails the migration and must be fixed at the shared seam—not skipped. Run the deterministic queue race, compact/handoff event invariant, engine fixture, and subprocess scenarios. Preserve Task 23’s implemented run-option/override behavior only; unproven native harness propagation remains `unverified — confirm first`, not an invented feature claim.

## Critical files & anchors

- **Immutable evidence and ownership:** comparison `D:/Projects/takopi/docs/reference/untether-comparison.md`; read-only Takopi source `D:/Projects/takopi` at `481f0c72f50ef5b7a7793f547059d691f7b8ffc3`; read-only Untether reference `D:/Projects/untether-review` at `4285dad5a12e4e4113c9cc5240972a67bbb5e218`; writable final destination `D:/Projects/untether`, verified this planning session clean at the same Untether SHA.
- **Scheduler source/destination:** `D:/Projects/takopi/src/takopi/scheduler.py` → `src/untether/scheduler.py`; destination consumers `src/untether/telegram/loop.py`, `commands/cancel.py`, `commands/executor.py`, `commands/dispatch.py`, `at_scheduler.py`, `loop_scheduler.py`, and `triggers/dispatcher.py`. Preserve the inspected 11-position `run_job(chat_id, user_msg_id, prompt, resume_token, context, thread_id, chat_session_key, reply_ref, on_thread_known, engine_override, progress_ref)` boundary or convert every named caller together to keywords.
- **Queue presentation:** `src/untether/telegram/bridge.py:render_progress` retains `now`, permission-button merging, and refresh settings; add queued/steered terminal controls with `untether:` callback prefixes. Preserve `src/untether/telegram/outbox.py` coalescing/priority/`SUPERSEDED` and flush-before-close behavior. Existing `tests/test_telegram_queue.py` is an outbox suite—do not overwrite it; put ported cancellation/steer tests in `tests/test_telegram_cancel_steer.py`.
- **Steering/control:** source `src/takopi/runner_bridge.py:RunnerTurnControl` and its `RunningTask.control` extraction/cancellation wiring; source runner emitters including `src/takopi/runners/codex.py`; destination `src/untether/runner_bridge.py:RunningTask` and applicable runner event bridge. A queue card exposes Steer only after this entire producer→RunningTask→callback chain exists.
- **Prompt/config:** source `src/takopi/telegram/prompt_batch.py`, `runners/run_options.py`, `runners/modes.py`, and `telegram/engine_overrides.py`; destination `src/untether/settings.py:TelegramTransportSettings` (`extra="forbid"`), `telegram/backend.py`, `telegram/bridge.py:TelegramBridgeConfig`, `telegram/loop.py`, `runners/run_options.py`, and existing command plugins. New batch settings must be added through all three destination config layers and classified for hot reload versus `RESTART_REQUIRED_FIELDS`.
- **Compact/ACP:** source `src/takopi/compact.py`, `runners/_compact_mixin.py`, `runners/_acp.py`; destination `src/untether/compact.py` (new), runner adapters, command plugins, scheduler job kinds, and public API. Reuse destination process management and event types.
- **Engines/schemas:** source `src/takopi/runners/{omp,grok,agy}.py`, `schemas/{pi,grok}.py`, `utils/transient_failures.py`; destination `src/untether/runners/`, `src/untether/schemas/`, `src/untether/engines.py`, `pyproject.toml [project.entry-points."untether.engine_backends"]`, and `src/untether/api.py`. Preserve Gemini and Amp entries and every existing backend.
- **Process boundary:** source `src/takopi/utils/subprocess.py`; destination `src/untether/utils/subprocess.py`, `utils/proc_diag.py`, runner/control/ACP/trigger spawn sites. Keep environment policy in `utils/env_policy.py` and audit in `utils/env_audit.py`; do not opt new engines into filtered environments without engine-specific proof.
- **Destination-only preservation anchors:** `triggers/{manager,dispatcher,server,cron,fetch,settings,ssrf,rate_limit,history,run_once_state,templating,auth,actions,describe}.py`; `cost_tracker.py`; `session_quarantine.py`; `config_watch.py`; `config_reload_notification.py`; `config_migrations.py`; `sdnotify.py`; `worktrees.py`; `plugins.py`; `runtime_loader.py`; `session_stats.py`; `telegram/{outbox,outbox_delivery,progress_persistence,state_store,offset_persistence,topic_state,chat_sessions,chat_prefs}.py`; and `tests/conftest.py` autouse quarantine/cancel-dedup isolation.
- **Platform/packaging:** `.github/workflows/ci.yml`, `pyproject.toml`, `LICENSE`, `src/untether/lockfile.py`, `tests/_subprocess_helpers.py` (new only if the same helper cannot be added to existing fixtures), `test_attestation_marker.py`, `test_exec_runner.py`, `test_claude_runner.py`, and `test_noop_resume_harness.py`. Preserve `.github/workflows/release.yml`, Dependabot, Bandit, pip-audit, build/install/TestPyPI/release validation. Update LICENSE to retain banteg and Little Bear Apps attribution and add Nathan Schram/Takopi attribution from the inspected source license.

## Verification

Run from the writable destination `D:/Projects/untether` with `PYTHONUTF8=1`; use `uv sync --frozen` before `--no-sync` commands. Never run these commands from or write generated output into `D:/Projects/takopi` or `D:/Projects/untether-review`.

1. **Focused red/green slices:**
   - `uv run --no-sync pytest tests/test_scheduler_queue.py tests/test_telegram_cancel_steer.py tests/test_telegram_bridge.py -q --no-cov`
   - `uv run --no-sync pytest tests/test_telegram_prompt_batch.py tests/test_telegram_prompt_batch_integration.py tests/test_runner_run_options.py tests/test_image_support.py tests/test_plan_goal_modes.py -q --no-cov`
   - `uv run --no-sync pytest tests/test_compact.py tests/test_compact_runner.py tests/test_telegram_compact.py tests/test_telegram_handoff.py tests/test_acp_compact.py -q --no-cov` (use the exact adapted filenames if destination convention differs; record that mapping in the commit)
   - `uv run --no-sync pytest tests/test_pi_schema.py tests/test_pi_runner.py tests/test_grok_schema.py tests/test_grok_runner.py tests/test_agy_runner.py tests/test_transient_failures.py -q --no-cov`
   - `uv run --no-sync pytest tests/test_subprocess.py tests/test_subprocess_close.py tests/test_proc_diag.py -q --no-cov`
2. **Concrete queue race proof:** using deterministic AnyIO barriers, hold active A, enqueue B then C with the same `ResumeToken`, invoke cancellation with B’s `(chat_id, progress_message_id)`, release A, and observe `CancelQueuedResult(CANCELLED, B)`, execution order `[A, C]`, no B invocation, A unaffected, B’s terminal card labelled cancelled with no controls, and a repeated callback returning `NOT_FOUND` without touching A or C. Repeat with cancellation after worker claim and observe `ALREADY_CLAIMED` plus exactly one B execution.
3. **Concrete compact/handoff proof:** feed a stored engine JSONL fixture through compact/handoff; observe exactly one `StartedEvent`, zero-or-more `ActionEvent`, exactly one final `CompletedEvent`, identical Started/Completed `ResumeToken`, full summary used to seed the new handoff session, future session routing changed only after successful seeding, and cost/stats/progress/outbox delivery hooks each invoked once.
4. **Concrete decoder/engine proof:** decode an unknown Pi event to `PiUnknownEvent`, reject a malformed known event, accept floating `delayMs`, and run OMP fixture IDs through start/progress/completion to observe the complete unshortened ID. Stored Grok/Agy capacity fixtures must produce one user-safe failure and no raw JSON; retry fixtures must show no retry after an action side effect.
5. **Untether preservation suites:** run the existing focused tests for trigger server/manager/dispatcher/cron/SSRF/auth/actions/fetch/rate-limit/settings/history; cost tracker; quarantine; config watch/reload/settings; outbox/delivery/files; command/callback registry; env policy/audit; doctor; sdnotify/drain/shutdown; config store/migrations; persistence/topic/session/chat prefs; stats/usage; worktrees/git; plugins/runtime/engine/transport discovery; all pre-existing runners; `at_scheduler`; and `loop_scheduler`.
6. **Static/full gates:**
   - `uv run --no-sync ruff format --check src tests`
   - `uv run --no-sync ruff check src tests`
   - `uv run --no-sync ty check src tests` → zero diagnostics.
   - `uv run --no-sync pytest tests/ -q --no-cov` on Windows, macOS, and Linux/Python 3.14; Ubuntu additionally runs supported Python 3.12 and 3.13 cells.
   - `uv run --no-sync pytest tests/ -q` on Ubuntu/Python 3.14 → all tests pass and branch coverage is at least 81%.
7. **Packaging/security/docs:**
   - `uv lock --check`
   - `uv build`
   - `uvx twine check dist/*`
   - `uvx check-wheel-contents dist/*.whl`
   - install the built wheel in the existing clean install-test environment and import Untether plus all registered engine/transport/command backends.
   - `uv run --no-sync bandit -r src/ -c pyproject.toml -q`
   - `uv run --no-sync pip-audit --skip-editable --progress-spinner=off`
   - `uv run --group docs zensical build --clean`
   - Open the PR and require every existing release/security job plus the new three-OS checks/test matrix to pass.
8. **Live optional gates:** run OMP/Grok/Agy/ACP native probes only with their explicit CLI/provider credentials and opt-in environment variables. A missing credential is `unverified — confirm first`, never a skipped deterministic test or a positive native-capability claim.

## Assumptions & contingencies

- Direction and baseline are fixed: Takopi → Untether. If either immutable SHA is unavailable or the destination has moved, stop and regenerate the affected comparison before editing.
- Graphify indexes are absent in both repositories. `ast-index v3.45.0` is installed but `stats`, `conventions`, and `map` report `Index not found`; `agrep` reports missing `ast-grep`. Rebuilding would write index artifacts and was correctly avoided in read-only planning. During writable execution, run `ast-index rebuild` only if its generated index is ignored/non-product state; otherwise continue with LSP/AST-grep/direct inspection. This tool limitation does not weaken the source/test evidence already captured.
- Untether’s current Windows run is not green: the inspected baseline had 18 collection errors and 129 failures. Fix root causes before enabling a required Windows cell: guard `src/untether/lockfile.py`’s module-level `fcntl`, replace Bash-only attestation invocation with a platform-capable path or narrowly POSIX-skip that script-only assertion, create Windows `.bat` wrappers for executable-script runner fixtures, and make `os.X_OK` assertions platform-aware. Never mark the whole Windows job `continue-on-error` or skip whole features in the finished gate.
- Hard `ty` is a final acceptance criterion, not an intermediate CI switch. Clear the measured 348 destination diagnostics before removing `allow_failure`; do not merge a knowingly red required job.
- Measure destination coverage before changing the threshold. If it is below 81%, add the smallest behavioral tests covering migrated branches, then set `--cov-fail-under=81`; do not lower or defer the final gate.
- Keep `sulguk>=0.11.1` and all other destination dependency floors unless a ported source import demonstrably requires a higher installed version. Prefer adapting to existing dependencies; any lockfile change must be a direct consequence of a required runtime API.
- `ResumeToken.is_continue` remains part of Untether’s persisted/invocation model but not scheduler equality. Add regression tests before changing any serializer or equality use; if a destination caller demonstrably relies on dataclass equality including `is_continue`, use an explicit scheduler key `(engine, value)` rather than changing model equality.
- `TelegramTransportSettings` forbids extras. Add prompt-batch fields to the model and hot-reload plumbing; classify them as hot-reloadable unless inspected behavior requires restart. Preserve the existing restart-required set for bot token, chat ID, session mode, topics, and message overflow.
- New engines inherit Untether’s current environment behavior (`env() -> None`) unless their source runner explicitly and safely uses the destination filtered-env contract. Preserve Claude/Pi-only filtering and Linux-only best-effort audit.
- Preserve the 11-argument `run_job` boundary initially. If run options make positional growth unavoidable, replace it once with a destination-owned request dataclass only if it removes more coupling than it adds; otherwise pass new metadata inside `EngineRunOptions`/`ThreadJob` and leave all trigger/at/loop callers stable. Ponytail default: leave the signature stable.
- Task 23 is only partially implemented at the source snapshot. Port its proven `EngineRunOptions`, `EngineOverrides`, precedence, and tests. Compact/handoff/ACP behavior covered by source modules and deterministic tests—including destination-before-approval, queued confirmation, event ordering, and session rerouting—is required. Only unproven credentialed native-harness propagation/capability remains `unverified — confirm first`; never use that label for the deterministic contract.
