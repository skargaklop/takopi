# Takopi → Untether Complete Gap Audit and Roadmap Plan

## Context

Audit every commit in `D:/Projects/takopi` and every Takopi→Untether migration artifact against the current `D:/Projects/untether` implementation, then record every still-missed migration feature without treating stale historical prose as current truth. Add the confirmed gaps to Untether’s roadmap, including Takopi-compatible TOML logging, visible plan/goal mode indicators beside the session/context identity, complete split delivery for long Telegram answers, Groq plus `D:/Projects/AI-Video-Transcriber` local voice transcription, and Codex mid-turn steering. Execution must preserve unrelated work and commit each independently reviewable step.

The `/health` failure is now part of this same migration-gap scope and every prior task ledger; it is not a replacement plan. Source registration and rendering exist, but installed entry-point discovery, generic slash dispatch, built-wheel contents, Startup-owned delivery, and process survival were not proven. The fatal live traceback came from a removed stale wheel path and is already corrected in current source (`ResolvedMessage.engine_override=directives.engine`), so implementation must follow `local://slash-health-migration-gap-plan.md`: add engine-directive, real registry, end-to-end `/health`, degradation, and wheel-install regressions; carry the partial/runtime-unverified gap into `ROADMAP.md`, the migration audit, docs, changelog, verification gates, and independent commit sequence; then restart exactly one editable-project poller and run an authorized `/health` → engine directive → `/health` smoke. Causation of the exact reported slash attempt remains `runtime-unverified` until that smoke.

## Approach

Discovery in progress. This section will be replaced incrementally with exact audit, roadmap, test, and commit steps after all commit-history slices, migration artifacts, current Untether behavior, and the local transcriber CLI have been inspected.

### Interim evidence ledger — preserve through compaction

This ledger is the durable scratchpad for the audit. Execution must re-confirm branch, revision, and dirty state, then preserve unrelated work. No repository or system state was changed while collecting this evidence. Each implementation slice below ends with its own focused verification and commit; never combine completed slices merely to reduce commit count.

#### Commit and migration coverage already established

- The authoritative ordered Takopi population is `git log --all --reverse --format=%H`. A fresh read-only `git rev-list --count --all` reports **453 reachable commits**, from `75fa95752feac42d05dc635c450027c69aa6ae17` through `d28e5ba7cf449608634c4a3ab6206998e6d4f0ae`. Twelve read-only subagents own disjoint ordinal ranges `1–38`, `39–76`, `77–114`, `115–152`, `153–190`, `191–228`, `229–266`, `267–304`, `305–342`, `343–380`, `381–418`, and `419–453`. Each must inspect every owned commit diff rather than its subject alone and return one commit-ledger row. Merge only after proving 453 unique hashes, no ordinal gaps/overlaps, matching range boundaries, and a disposition for every commit.
- For every commit classify behavioral feature/fix/security/compatibility; test/docs evidence; release/build/refactor/churn without an independent current contract; or removal/supersession. Deduplicate related commits into stable feature keys while retaining every contributing hash. Trace removals, reversions, and refinements to final Takopi behavior; never promote obsolete intermediate behavior into a current migration requirement.
- Add the merged exhaustive **Takopi feature and requirement inventory** to this plan. Every deduplicated feature row must state the observable contract and edge cases, contributing hashes, final Takopi source/tests/docs anchors, one Untether disposition (`implemented`, `partial`, `missing`, `runtime-unverified`, `superseded`, `not-applicable`, or `stale`), exact Untether source/tests/docs anchors, and the focused verification required to prove equivalence. `implemented` requires grounded behavior, security constraints, cancellation/error handling, applicable cross-platform behavior, and regression evidence; similar names are insufficient.
- Convert every `partial`, `missing`, `stale`, and actionable `runtime-unverified` row into an explicit implementation/verification task and, when user-facing or deferred, a `ROADMAP.md` entry. Preserve superseded/not-applicable rows with rationale. Reconcile the exhaustive ledger against every migration plan, both roadmaps/changelogs, and `docs/audits/2026-08-09-takopi-feature-port-audit.md`; commit-derived final source behavior wins when prose conflicts. Earlier 72-entry/13-family sampling is retained only as provenance and cannot substitute for this direct 453-commit audit.
- Authoritative historical inputs already inspected include `D:/Projects/takopi/docs/plans/2026-08-08-takopi-to-untether-migration.md`, `D:/Projects/takopi/docs/plans/2026-08-09-takopi-untether-audit-cutover-plan.md`, `D:/Projects/takopi/docs/reference/untether-comparison.md`, `D:/Projects/takopi/ROADMAP.md`, and Untether’s `docs/audits/2026-08-09-takopi-feature-port-audit.md`, `ROADMAP.md`, and session-local gap plans. Current Untether source/tests override stale completion prose.
- Confirmed remaining gaps are Takopi-compatible TOML `[logging]` (explicit current user requirement despite the earlier cutover plan’s env-only decision), production wiring for visible `plan`/`goal` indicators, Codex app-server steering, enhanced voice transcription, OMP/Grok failure handling, explicit `/stats` observability, and installed `/health` reliability. Long-answer splitting is already shipped and must be regression-proven, not reimplemented.
- Preserve prior verified Windows migration evidence: focused migration tests `529 passed, 15 skipped`; resolver tests `9 passed`; full suite `3227 passed, 35 skipped`; Windows branch coverage `78.26%`; `uv run --no-sync ty check src tests` passed; format/Ruff, lock, audit, docs, build, wheel/source-import, and doctor checks passed. These are historical observations, not substitutes for post-change gates.
- Preserve prior independently reviewable Windows commits: `b0ae3ce`, `41ab4a3`, `7868154`, and `834533b`. New work starts from a re-confirmed checkout and adds one commit after each completed, focused behavior slice.

#### Logging compatibility

- The migration artifacts conflict: the 2026-08-09/10 cutover plans intentionally removed `[logging]` and retained only `TAKOPI_LOG_*`, while current user scope explicitly requires Takopi-compatible TOML logging. The user requirement wins. Correct audit row D2 from `superseded` to `missing/requested`; do not rewrite or remove the established environment variable names.
- Restore `LoggingSettings(level, file, format)` on `UntetherSettings` and feed it into `setup_logging()` during normal CLI startup. Exact precedence is `--debug` > `TAKOPI_LOG_LEVEL` > TOML > defaults for level; `TAKOPI_LOG_FILE` and `TAKOPI_LOG_FORMAT` override TOML. Resolve relative files below `~/.untether`, append UTF-8, retain Untether’s stronger Telegram/OpenAI/GitHub-token redaction plus color/trace controls, and disable only a failing file sink nonfatally. Configuration remains restart-applied.
- Test first in `tests/test_settings.py` and logging tests: defaults, valid/invalid TOML, all precedence branches, relative/absolute file resolution, append behavior, redaction, and file-open failure. Implement through `src/untether/settings.py`, `src/untether/logging.py`, and `src/untether/cli/run.py`; document in `docs/reference/config.md` and env reference. Commit as one focused logging compatibility slice.

#### Plan/goal presentation gap and long responses

- `format_mode_badge()` and `compose_context_line()` exist in `src/untether/directives.py` but have no production consumers. Current production uses `format_context_line()` from `telegram/loop.py`, `telegram/commands/cancel.py`, and `telegram/commands/executor.py`. Wire the existing helpers at queued, running, cancelled, and completed progress/session-card seams. Render goal ahead of plan when both are present; do not invent skill/subagent badges.
- Enabled `message_overflow = "split"` must preserve the complete answer as ordered Telegram messages, with entity/code-fence-safe chunks, stable reply/thread identity, exactly one context/resume footer, and controls only on the final part. Keep explicit `trim` only when configured. Existing behavior is believed shipped; add/retain observable regression coverage rather than adding a second splitter.

#### Codex mid-turn steering gap

- Takopi’s verified contract was introduced by commit `7cadc477e4ace67bca31384737dce80d604e5b96` (`feat: use codex app-server by default`, 2026-05-15). It uses Codex `app-server` by default and retains `codex.mode = "exec"` as a compatibility fallback. `_AppServerClient` owns JSON-RPC request correlation and per-turn subscriptions; `_AppServerTurnControl.steer()` calls `turn/steer` with `threadId`, `expectedTurnId`, and one text input, while `interrupt()` calls `turn/interrupt`. `AppServerCodexRunner` publishes that control in `StartedEvent.meta["control"]`. Untether copied `RunnerTurnControl`, scheduler claim/requeue, Telegram `steer`/`cancel`, and queued-card markup, but its current `CodexRunner` is exec-only; `run_runner_with_cancel()` neither extracts control metadata nor invokes `control.interrupt()` on cancellation. The visible steer path is therefore unreachable and turn-aware cancellation is absent: disposition `partial` across runner and bridge, not merely runner-local.
- Port the Takopi app-server implementation into `src/untether/runners/codex.py` using Untether names/logging/security/subprocess helpers, with `app_server` as the default and explicit `exec` fallback. Preserve one client per runner, lazy initialize, `thread/start`/`thread/resume`, `turn/start`, notification routing, commentary/plan translation, transient-error sanitization without replay after side effects, bounded shutdown, Windows process-tree cleanup, and explicit stream closure. Never emulate steering by cancelling and resubmitting.
- In `runner_bridge.py::run_runner_with_cancel`, when the internally produced `StartedEvent.meta["control"]` is non-`None`, assign it to the existing `RunningTask.control` alongside the resume assignment, before `on_thread_known` and `resume_ready`. Keep the control runtime-only: never persist it or render it through `ProgressTracker`. In `wait_cancel`, call `control.interrupt()` best-effort before cancelling the bridge task group, preserving cancellation even if interrupt raises. Preserve exact resume matching: steering may claim only the queued job whose `ResumeToken` equals the active task. On `turn/steer` success consume the job exactly once and edit only its queued card to terminal `steered`; retain the active control so another same-turn prompt may steer. On RPC failure requeue the job at the front and keep the active turn/control intact. Stale, duplicate, already-claimed, wrong-thread, and exec-mode callbacks remain safe and visible. The control naturally disappears when `handle_message()` removes the `RunningTask` in `finally`.
- Port/adapt Takopi’s deterministic contracts into Untether tests: config default/validation and exec fallback; app-server initialize/start/resume/turn and notification translation; exact `turn/steer` and `turn/interrupt` requests; `StartedEvent.meta` control; `run_runner_with_cancel` attaches control before `on_thread_known`/`resume_ready` and invokes interrupt on cancel; callback claim-before-await preventing duplicate execution; failure requeue; same-thread isolation; steer/cancel markup only when a real control exists; server EOF during an active turn becomes a visible runner error; `stop()` kills the tree and closes streams. Add config/reference, audit, changelog, and roadmap entries. Commit the runner protocol/config first and bridge/Telegram activation second only if each is independently green; otherwise keep one atomic steering commit.

#### Shared OMP/Grok failure

- Earlier source/log inspection found `JsonlSubprocessRunner._iter_jsonl_with_timeouts()` calling `stdout.readline()` against AnyIO `StreamReaderWrapper`, and the existing timeout test used a matching invalid `readline()` fake. A later scout at Untether `834533b` reports that current `runner.py:1036-1068` already distinguishes startup/idle timeouts and no longer identifies this as the active defect. Treat this as source drift: execution must re-read the current function and test before editing. If `readline()` remains, repair it through `iter_json_lines()` and replace the fake; if already repaired, record that historical defect as implemented and do not rewrite it.
- The current confirmed OMP/Grok retry defect is one shared classification boundary with asymmetric runner plumbing: `JsonlSubprocessRunner.run_impl()` classifies only `CompletedEvent.error`. Pi/OMP `process_error_events()` includes a sanitized stderr excerpt, so retryable 503 text reaches `classify_transient_failure()`; Grok `process_error_events()` discards stderr and emits only `grok failed (rc=N).`, so rc!=0 429/503 failures cannot classify, retry, or receive sanitized transient formatting. Grok stream `error` events do carry the message; `StreamEndEvent(stopReason="error")` currently lacks a proven error-message field.
- Add deterministic tests before code: Grok rc!=0 with sanitized stderr `Internal error`/503 produces the existing retry note, retries only before visible start/action/answer, then emits `format_transient_failure()` without raw provider JSON; non-transient exits remain one attempt; OMP bare-503 behavior remains unchanged. Update `src/untether/runners/grok.py::process_error_events` to follow Pi’s bounded `_stderr_excerpt` pattern and `src/untether/runner.py::run_impl` only if the failing shared-boundary test requires it. A live Grok end-event shape remains `runtime-unverified`.

#### OMP large-prompt transport

- The prompt boundary is now source-documented, not inferred: `D:/Projects/omp-docs-20260702/cli.md:90-102` states that one-shot `-p` reads standard input into the prompt, including `cat README.md | omp -p "Summarise"`; JSON/RPC is a separate protocol. Current `src/untether/runners/omp.py::OmpRunner.build_args()` always appends the sanitized prompt to argv, while the inherited `JsonlSubprocessRunner` already supports a `stdin_payload()` hook and sends/closes that payload before consuming JSONL. This leaves Windows command-line-length and batch-wrapper failure risk for large/multiline prompts.
- Test first in `tests/test_omp_runner.py`: the effective goal/soft-plan prompt, including multiline and a payload larger than the Windows command-line budget, is absent from `build_args()` and returned as newline-terminated UTF-8 by `stdin_payload()`; single-line prompts follow the same stdin-only boundary to avoid two transport modes; resume/model/provider/thinking/attachment flags remain ordered and unchanged; sanitization invariants remain covered; the subprocess receives exactly one prompt and stdin closes before JSONL consumption. Implement one `_final_prompt()` helper in `OmpRunner`, use it from both methods, remove the trailing prompt argv append, and do not introduce a prompt file, shell pipeline, RPC client, or engine-specific subprocess loop. Run focused OMP/JSONL runner tests on Windows and retain POSIX behavior.
#### Claude startup stall

- Current source trace inspected: `src/untether/runners/claude.py::run_impl()` builds args, opens `manage_subprocess(...)`, sends stdin, merges stdout/stderr readers in an AnyIO task group, and emits `_silence_events(...)` while waiting. Existing logs contain normal Claude completion evidence, but no observed traceback in the inspected excerpts that alone proves the reported startup stall.
- Treat the Claude symptom separately from the confirmed OMP/Grok `readline` defect. Final diagnosis must name the exact startup boundary, deterministic failing fixture, and observed error before proposing a source change; absent a reproducing log/test remains `runtime-unverified` rather than guessed.

- Claude already has source/test contracts for the two “looks hung” classes: cold-start zero-byte Type B stream idle timeout and post-tool-result wedges. `tests/test_claude_runner.py` verifies `CLAUDE_STREAM_IDLE_TIMEOUT_MS` default/override/config wiring and Type A/Type B extraction; `settings.py::WatchdogSettings` exposes `detect_stuck_after_tool_result`, recovery delay, catalog staleness, and Claude stream idle timeout. Diagnosis must first correlate the user’s observed stall with one of these event signatures; adding another timeout before that would duplicate existing mechanisms.
#### Voice transcription implementation

- Implement this in the current migration, not roadmap-only. `avt` is installed at `D:/Projects/AI-Video-Transcriber/.venv/Scripts/avt.exe`, so the current implementation must use that executable rather than adding a second local transcription stack to Untether. Reuse `src/untether/telegram/voice.py::VoiceTranscriber.transcribe(*, model, audio_bytes, language=None) -> str` and the existing `transcribe_voice()` download/size/SSRF/error chokepoint; do not create a second Telegram voice path. Preserve the current OpenAI behavior as the compatibility default and allow Groq as a remote provider that consumes no local model storage.
- Extend `TelegramTransportSettings` with `voice_transcription_provider: Literal["openai", "groq", "local"] = "openai"`, `voice_transcription_groq_api_key: SecretStr | None`, `voice_transcription_local_command: NonEmptyStr = "D:/Projects/AI-Video-Transcriber/.venv/Scripts/avt.exe"`, `voice_transcription_local_backend: Literal["whisper", "parakeet"] = "whisper"`, `voice_transcription_local_model: NonEmptyStr = "base"`, and bounded positive `voice_transcription_timeout_s` defaulting to 180. Preserve existing language/API-key validators, hot reload, and environment overrides. Do not add provider/model fallback: one configured local backend/model is authoritative, preventing fallback downloads of additional models.
- For `groq`, instantiate the existing OpenAI-compatible transcriber with `https://api.groq.com/openai/v1`, the Groq `SecretStr` value only at the transport boundary, and default model `whisper-large-v3-turbo` when no explicit provider model override exists. Describe Groq as free-tier available, never guaranteed free. Keep custom OpenAI `base_url` on the existing DNS/SSRF allowlist path; the fixed public Groq endpoint is not user-controlled.
- Add `AvtVoiceTranscriber` behind the same protocol. Write downloaded bytes to a private temporary `.ogg`, invoke the installed executable with `--quiet transcribe --file <path> --provider local --local-backend whisper --local-model <configured>` or the configured Parakeet form, feed no secret/environment credential on argv, bound the subprocess with AnyIO, capture bounded stdout/stderr, require exit zero plus a JSON object with non-empty string `transcript`, preserve cancellation, and unlink the temporary file in `finally`. Do not import `D:/Projects/AI-Video-Transcriber` modules or couple to its FastAPI server; its registered `avt = "cli:main"` JSON CLI is the stable boundary.
- Startup/doctor resolution is deterministic: validate the configured executable first; if it is unavailable, report local transcription unavailable and then implement a single-model Untether-local adapter only as a contingency task—never silently download or select another model. That contingency must use one configured model ID and one explicit cache directory shared with the configured local runtime, with no preset probing, model cycling, or automatic fallback. Do not delete existing caches during migration; report discovered cache locations/sizes and require separate operator approval for cleanup.
- Planning-time cache inventory confirms the disk concern: AVT has Parakeet v2 (**631 MiB**) and v3 (**640 MiB**) under `D:/Projects/AI-Video-Transcriber/models/onnx_asr`, while Hugging Face has faster-whisper base (**142 MiB**) and large-v3 (**2.9 GiB**) under the user cache—about **4.3 GiB** total across four local models. No cache is deleted during planning or implementation without explicit operator approval. Configure exactly one local model for Untether and add a read-only doctor report naming backend/model/cache size so the operator can choose which three obsolete caches to remove separately.
- Test first in `tests/test_settings.py`, `tests/test_telegram_voice.py`, `tests/test_bridge_config_reload.py`, and `tests/test_cli_helpers.py`: compatibility defaults; schema/secret masking; installed-AVT discovery and missing-executable doctor failure; Groq endpoint/model/key boundary; exact single-model Whisper and Parakeet argv; JSON success; nonzero/malformed/empty output; timeout and cancellation; private-temp cleanup on every exit; size/language/SSRF behavior unchanged; hot reload; and proof that no alternate local backend/model is attempted or downloaded. Document the single-model disk policy and first-use download for only the configured model in `docs/reference/config.md`/env reference, update audit/roadmap/changelog, and commit settings/provider adapter plus docs as one independently green voice slice.

#### `/health` additive gap

- Canonical detailed plan remains `local://slash-health-migration-gap-plan.md`. Keep the entry-point-backed `HealthCommand` and generic dispatcher; never add a hard-coded branch or invoke engine resolution/runner.
- Selected UX is bounded progressive delivery: send immediate HTML alive/uptime/active/queued/trigger summary plus `collecting diagnostics…`, retain its `MessageRef`, collect independent detailed diagnostics concurrently with per-collector and overall deadlines, use `anyio.to_thread.run_sync(...)` for blocking OS/process probes, then edit the same message with Service/Process/System/Usage/Diagnostics sections. Unsupported or failed fields render `unavailable`; cancellation propagates; never catch `BaseException`; edit failure leaves the initial response visible and logs only sanitized timing/error metadata.
- Add narrow immutable aggregate health values through `CommandContext`, not direct `RunningTasks`/scheduler exposure or a global watchdog registry. Measure slash-to-initial-send, each collector, total detailed collection, and final edit. Tests prove initial send precedes a blocked collector, same-message edit, timeout isolation, edit-failure survival, HTML reply linkage, and zero resolver/runner calls.
- Fatal live evidence came from stale already-loaded wheel code using `engine_override=engine_override`; current source uses `directives.engine`, and the editable `.pth` targets source. The historical stale-metadata → slash fallthrough → stale resolver crash chain remains `runtime-unverified`. Restart exactly one Startup-owned poller only during authorized execution.

#### Commit discipline

- Every completed intermediate task gets its own commit immediately after its focused tests pass. Planned boundaries: audit/roadmap corrections; logging compatibility; plan/goal presentation; long-answer regression only if code changes; Codex app-server protocol/config; Codex runtime-control attachment/Telegram steering; shared JSONL/Grok retry repair; Claude repair only if a reproduced distinct defect exists; `/stats` contract/tests; voice provider/local-adapter work if implementation is in scope rather than roadmap-only; `/health` resolver/dispatch/degradation/package slices; final docs only where coupled behavior cannot be documented in the behavior commit.
- Never commit planning drafts or unrelated user changes merely to satisfy commit cadence. During the current read-only planning phase, interim evidence is saved in this session-local plan; repository commits begin only after plan approval and execution mode starts.


Confirmed configuration gap: Untether’s permissive top-level settings model accepts `[logging]` into unused extras, while `setup_logging()` and CLI startup consume only the established `TAKOPI_LOG_*` environment controls. Restore Takopi’s strict `LoggingSettings` (`level`, `file`, `format`) and startup wiring; use tested Takopi code semantics where its prose conflicts (relative files under the application home, therefore `~/.untether`, not CWD). Preserve precedence `--debug` > `TAKOPI_LOG_LEVEL` > TOML > defaults for level, environment-over-TOML for file/format, append/UTF-8 behavior, redaction, color/trace controls, and nonfatal disabled sink on file I/O failure. Logging config remains restart-applied, matching Takopi; it is not a hot-reload gap.
Confirmed presentation gap: Takopi routes `plan`/`goal` through `Runtime.compose_context_line(...)` at queued, claimed/cancelled, and active executor presentation seams, rendering `` `plan` `` or `` `goal` `` immediately before the session context line. Untether currently contains copied helper functions in `src/untether/directives.py` but no production callsites, so helper-only tests do not implement the user-visible indicator. The final plan must add this as a distinct roadmap item and specify every progress/session-card lifecycle callsite plus observable presentation tests; exact user wording (“near session name”) must be preserved while reusing Untether’s existing context/resume footer rather than inventing a second session header.

Explicit overflow requirement: when the split mode is enabled, model answers larger than Telegram’s per-message limit must be delivered completely as an ordered multi-message sequence, not shortened. Takopi’s schema defaults `message_overflow` to `"split"` and constructs `TelegramPresenter(message_overflow=...)`; the final plan must verify whether Untether preserves the entire presenter→transport multi-payload contract rather than treating the settings field alone as parity. Acceptance must cover entity/code-fence-safe chunking, ordering, reply/thread identity, exactly one resume/context footer, terminal controls on the final part, and explicit `"trim"` compatibility only when an operator chooses it.

Confirmed shared JSONL timeout regression, now reproduced by both OMP and Grok in the `obsidian` directory after about 11 seconds: `JsonlSubprocessRunner._iter_jsonl_with_timeouts()` in `src/untether/runner.py` directly awaits `stdout.readline()`, but `anyio.open_process()` supplies a `StreamReaderWrapper` implementing AnyIO’s byte-receive interface, not asyncio’s `readline()` API. Any `JsonlSubprocessRunner` with both startup and idle deadlines enabled selects this broken branch; the ordinary no-timeout branch correctly routes the same stream through `iter_json_lines()` → `iter_bytes_lines()` → `BufferedByteReceiveStream.receive_until()`. The engine and directory are not causal. Repair the shared timeout iterator by timing reads from the existing AnyIO line iterator, never through engine-specific workarounds or an assumed asyncio API. Preserve deterministic startup-vs-idle timeout classification, clean EOF, malformed-event recovery, cancellation, and watchdog behavior for Grok, OMP, Pi, Codex, OpenCode, Gemini, Amp, and other JSONL subclasses.

Confirmed health-runtime gap: `/health` is registered in `pyproject.toml`, advertised in startup command-menu logs, and uses the generic command backend. Current source dispatches registered slash commands before `resolve_message()` and its renderer degrades missing `/proc`, diagnostics, triggers, cost, and uptime. Existing tests exercise only `HealthCommand` directly; they do not cover installed metadata, loop dispatch, HTML delivery, no-runner behavior, or wheel contents. A fatal runtime `NameError` killed the bot from a stale loaded wheel while current source and the current editable venv are repaired. Add the full `local://slash-health-migration-gap-plan.md` work to all previous issues/tasks and preserve a distinct commit boundary from logging, voice, mode badges, JSONL, and stats work.

## Critical files & anchors

- `src/untether/settings.py`: add strict `LoggingSettings` and mount it on `UntetherSettings`; preserve unrelated permissive plugin/config behavior.
- `src/untether/logging.py`: restore optional config parameters and `~/.untether` relative-file resolution without regressing redaction or environment controls.
- `src/untether/cli/run.py`: resolve logging settings non-fatally at normal run startup and pass them into `setup_logging`; keep doctor/onboarding behavior and debug defaulting.
- `tests/test_settings.py`, `tests/test_logging.py`, `tests/test_logging_redaction.py`: validate defaults, TOML values/errors, precedence, relative/absolute paths, file append, failure behavior, and retained redaction.
- `docs/reference/config.md`, `docs/reference/env-vars.md`, `docs/audits/2026-08-09-takopi-feature-port-audit.md`, `CHANGELOG.md`, `docs/reference/changelog.md`: document the restored table and correct D2’s false “superseded” classification.
Discovery in progress.

- `src/untether/runner.py`, `tests/test_runner_utils.py`, `tests/test_exec_runner.py`, and focused Grok/OMP coverage: replace the current fake `readline()`-only timeout fixture with a real AnyIO `StreamReaderWrapper` regression test that currently fails with the exact missing-`readline` error while both timeout settings are enabled; then make `_iter_jsonl_with_timeouts()` consume `iter_json_lines(stdout)` one item at a time under the existing startup/idle deadlines. Test first-line and subsequent-line success, clean EOF, startup timeout, idle timeout, malformed JSONL through `_iter_jsonl_events`, cancellation/closed-stream behavior, and shared execution through both `GrokRunner` and `OmpRunner`; rerun affected JSONL runner, completion, cancellation, and watchdog suites.

- `src/untether/telegram/commands/health.py`, `src/untether/telegram/commands/dispatch.py`, `src/untether/telegram/loop.py`, `src/untether/transport_runtime.py`, `tests/test_health_command.py`, `tests/test_transport_runtime.py`, the existing loop/registry test modules, `pyproject.toml`, and `.github/workflows/ci.yml`: prove real installed registration, `/health` early dispatch and HTML send without runner resolution, graceful data-source failures, the engine-directive resolver regression, and built-wheel command-backend completeness. Carry the result into roadmap/audit/docs/changelog and finish with a single-poller authorized live smoke.


## Assumptions & contingencies

- “All Takopi commits” means all 453 commits reachable from current Takopi `HEAD`, including reverted/refactored behavior; final gaps are based on net user-visible contracts, not raw commit-message counts.
- Roadmap entries describe future Untether work; this request does not implement logging or transcription runtime behavior.
- The mode indicator is not subsumed by generic plan/goal parsing: completion requires visible `` `plan` ``/`` `goal` `` state adjacent to the existing session/context identity on queued, running, cancelled, and completed progress cards, with goal taking precedence when both flags are present.
- Long-answer completeness is an observable delivery contract: enabled `message_overflow = "split"` must emit every chunk in order without silently truncating content; a schema default or first-payload renderer test is insufficient.
- Health reliability is additive to every earlier migration issue and task. Source implementation alone does not close the gap: deterministic registry/loop/wheel tests and authorized Startup runtime evidence are required; absent Telegram authorization leaves only the runtime smoke `runtime-unverified`.
